name: default
kind: pipeline
type: docker
steps:
- name: docker
  image: plugins/docker
  settings:
    secrets: [ DOCKERUSERNAME, DOCKERPASSWORD ]
    repo: thebrones/apachestack
    tags: 
    - dev
    #- droneci
  when:
    branch: [ dev ]
    event: [ push ]