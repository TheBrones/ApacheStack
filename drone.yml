name: default

kind: pipeline
type: docker
steps:
  - publish: publish
      image: plugins/docker
      settings:
        secrets: [ DOCKERUSERNAME, DOCKERPASSWORD ]
        repo: thebrones/apachestack:dev
        tags: 
        - dev
        - droneci
      when:
        branch: [ dev ]
        event: [ push ]