name: default

kind: pipeline
type: docker
steps:
  - publish:
      image: plugins/docker
      settings:
        secrets: [ DOCKERUSERNAME, DOCKERPASSWORD ]
        repo: thebrones/apachestack
        tags: dev
      when:
        branch: [ dev ]
        event: [ push ]