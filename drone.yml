name: default

kind: pipeline
type: docker
steps:
# - name: docker  
#   image: plugins/docker
#   settings:
#     repo: https://hub.docker.com/r/thebrones/apachestack
#     dockerfile: Dockerfile
#     registry: github.com
#     tags: [ dev ]
  - publish:
      secrets: [ DOCKERUSERNAME, DOCKERPASSWORD ]
      image: plugins/docker
      settings:
        tags: dev
        repo: thebrones/apachestack
      when:
        branch: [ dev ]
        event: [ push ]